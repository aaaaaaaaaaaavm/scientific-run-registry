[build-system]
requires=["setuptools>=69","wheel"]
build-backend="setuptools.build_meta"
[project]
name="scientific-run-registry"
version="0.1.0"
description="Content-addressed registry for computational runs and evidence"
readme="README.md"
requires-python=">=3.11"
license={text="Apache-2.0"}
authors=[{name="Adityavardhan Mishra"}]
dependencies=[]
[tool.setuptools.packages.find]
where=["src"]
